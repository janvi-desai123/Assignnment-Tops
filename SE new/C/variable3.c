#include<stdio.h>
int main()
{
	int x = 5, y = 6, z = 50;	//declare multiple variable
	printf("%d %d %d",x,y,z );	
	const number=10;	// const keyword will declare the variable as "constant", which means unchangeable and read-only:		
	//number=15 it will genrate error because number is constant and we can't modify constant variable.
		
	return 0;
}
// syntax for const variable :  const variable_name= value;
