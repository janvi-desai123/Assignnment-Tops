#include<stdio.h>
int main()
{
	int side,area;
	printf("enter side");
	scanf("%d",&side);
	area=side*side;
	printf("one side of square is %d \n so area of sqaure is %d",side,area);
	
	
	return 0;
}
